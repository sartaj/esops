#!/usr/bin/env node
require('@esops/language-babel/node')

const sourceDir = `${__dirname}../cli`

// load the entry point or die trying
require(`${sourceDir}/lib/check.js`)

// run the CLI with the current process arguments
require(sourceDir)(process.argv)
