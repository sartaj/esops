export const resolveEsopsJson = () => []
export const normalizeStackShorthand = ([]) => [[[]]]
export const resolvePath = async arg => ''
export const asyncSequence = (asyncCallback, arr) => arr.forEach(asyncCallback)
export const asyncParallel = (asyncCallback, arr) => arr.forEach(asyncCallback)
export const esopsConfigExists = path => true
